<%= form_for(@webpage) do |f| %>
  <% if @webpage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@webpage.errors.count, "error") %> prohibited this webpage from being saved:</h2>

      <ul>
      <% @webpage.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pagename %><br />
    <%= f.text_field :pagename %>
  </div>
  <br />
  <div class="field">
    <%= f.label "Insert Your HTML Below" %><br />
    <%= f.text_area :html, :cols => 80, :rows => 14 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
