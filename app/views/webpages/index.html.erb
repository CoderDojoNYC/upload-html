<h1>Listing webpages</h1>

<table>
  <tr>
    <th>Pagename</th>
    <th></th>
    <th></th>
  </tr>

<% @webpages.each do |webpage| %>
  <tr>
    <td><%= webpage.pagename %></td>
    <td><%= link_to 'Show', webpage %></td>
    <td><%= link_to 'Edit', edit_webpage_path(webpage) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Create a New Webpage', new_webpage_path %>
